# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Claude Code: settings.json and the hooks are shared, the local overrides are not
.claude/settings.local.json

# Local dev
*.log

# Playwright MCP scratch + smoke screenshots (project root)
.playwright-mcp/
smoke-*.png

# Scheme test harness: the one playwright install the whole sub-app shares, and the oracle's
# captured dumps (18 MB of anim/dom baselines, regenerable with `npm run oracle:base`).
scheme/test/node_modules/
scheme/test/oracle/baselines/
