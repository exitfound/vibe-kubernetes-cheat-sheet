const ICON_NETWORK   = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="6" cy="6" r="2.5"/><circle cx="18" cy="6" r="2.5"/><circle cx="6" cy="18" r="2.5"/><circle cx="18" cy="18" r="2.5"/><line x1="8.5" y1="6" x2="15.5" y2="6"/><line x1="6" y1="8.5" x2="6" y2="15.5"/><line x1="8.5" y1="18" x2="15.5" y2="18"/><line x1="18" y1="8.5" x2="18" y2="15.5"/></svg>`;
const ICON_WORKLOADS = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="12 3 21 8 12 13 3 8 12 3"/><polyline points="3 13 12 18 21 13"/></svg>`;
const ICON_STORAGE   = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><ellipse cx="12" cy="5" rx="8" ry="2.5"/><path d="M4 5v6c0 1.4 3.6 2.5 8 2.5s8-1.1 8-2.5V5"/><path d="M4 11v8c0 1.4 3.6 2.5 8 2.5s8-1.1 8-2.5v-8"/></svg>`;
const ICON_CONTROL   = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4" y="4" width="16" height="16" rx="2"/><rect x="9" y="9" width="6" height="6"/><line x1="9" y1="2" x2="9" y2="4"/><line x1="15" y1="2" x2="15" y2="4"/><line x1="9" y1="20" x2="9" y2="22"/><line x1="15" y1="20" x2="15" y2="22"/><line x1="20" y1="9" x2="22" y2="9"/><line x1="20" y1="15" x2="22" y2="15"/><line x1="2" y1="9" x2="4" y2="9"/><line x1="2" y1="15" x2="4" y2="15"/></svg>`;

export const CATEGORIES = [
  { key: 'all',       label: 'All' },
  { key: 'control',   label: 'Cluster',       sub: 'Cluster internals',   icon: ICON_CONTROL },
  { key: 'workloads', label: 'Workloads',     sub: 'Pods and controllers', icon: ICON_WORKLOADS },
  { key: 'network',   label: 'Networking',    sub: 'Traffic flow',        icon: ICON_NETWORK },
  { key: 'storage',   label: 'Storage',       sub: 'Volume flow',         icon: ICON_STORAGE },
];

export const CATEGORY_ICONS = {
  network:   ICON_NETWORK,
  workloads: ICON_WORKLOADS,
  storage:   ICON_STORAGE,
  control:   ICON_CONTROL,
};

export const CATEGORY_SUB = {
  network:   'Traffic flow',
  workloads: 'Pods and controllers',
  storage:   'Volume flow',
  control:   'Cluster internals',
};

export const SCHEMES = [
  {
    id: 'network-model',
    title: 'Flat Pod Network Model',
    category: 'network',
    subcategory: 'foundations',
    desc: 'What does Kubernetes actually promise about the network? The model is one flat address space: every Pod gets a cluster-unique IP, any Pod reaches any other Pod on any Node with no NAT so the real source IP is preserved, and the Node agent reaches every Pod on its own Node. None of this lives in the core, a CNI plugin like Calico, Cilium or Flannel is what wires Pods into the flat space and upholds the rules.',
    k8sVersion: '1.35',
    module: './schemes/network-model.js',
    tinted: true,
    sources: [
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
      { label: 'The Kubernetes Network Model', href: 'https://kubernetes.io/docs/concepts/services-networking/#the-kubernetes-network-model' },
    ],
  },
  // Network Foundations runs from the promise to the machinery that keeps it: the flat model, the namespace
  // a Pod IP actually lives in, the two address ranges, then the dataplane that turns a Service address
  // into a Pod one (iptables/IPVS first, its eBPF replacement after) and the conntrack that pins a flow.
  {
    id: 'network-namespaces',
    title: 'Network Namespaces',
    category: 'network',
    subcategory: 'foundations',
    desc: 'What makes a Pod IP feel like a separate machine? A network namespace is a private copy of the Linux network stack: its own interfaces, routing table, iptables and ports. The pause container gets one that every container in the Pod shares, reaching the others over loopback, while a single veth pair is the only link to the host. Deleting the Pod releases the veth and IP together.',
    k8sVersion: '1.35',
    module: './schemes/network-namespaces.js',
    tinted: true,
    sources: [
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
      { label: 'CNI Specification', href: 'https://github.com/containernetworking/cni/blob/main/SPEC.md' },
    ],
  },
  {
    id: 'network-service-cidr',
    title: 'Service CIDR and ClusterIP Allocation',
    category: 'network',
    subcategory: 'foundations',
    desc: 'Where does a Service ClusterIP come from when nobody assigns it? The cluster carves it out of one configured Service CIDR that is split into two bands: a small low static band holds hand-picked, well-known addresses like 10.96.0.1 (the kubernetes API) and 10.96.0.10 (kube-dns), while a larger high dynamic band is where the allocator draws ClusterIPs automatically. Each allocation is tracked by an IPAddress object, and a second ServiceCIDR can be added to grow the range with no disruption.',
    k8sVersion: '1.35',
    module: './schemes/network-service-cidr.js',
    tinted: true,
    sources: [
      { label: 'Service ClusterIP Allocation', href: 'https://kubernetes.io/docs/concepts/services-networking/cluster-ip-allocation/' },
      { label: 'Extend Service IP Ranges', href: 'https://kubernetes.io/docs/tasks/network/extend-service-ip-ranges/' },
      { label: 'Service', href: 'https://kubernetes.io/docs/concepts/services-networking/service/' },
    ],
  },
  {
    id: 'network-dualstack',
    title: 'Dual-Stack IPv4 and IPv6',
    category: 'network',
    subcategory: 'foundations',
    desc: 'What changes when a cluster runs IPv4 and IPv6 side by side? With dual-stack enabled, the CNI gives every Pod one address per family on the same eth0, and a Service set to ipFamilyPolicy PreferDualStack gets one ClusterIP per family ordered by ipFamilies. ipFamilyPolicy also takes SingleStack, the default, or RequireDualStack, which fails the Service unless both families are available. A client resolves both A and AAAA records and connects over whichever protocol it prefers.',
    k8sVersion: '1.35',
    module: './schemes/network-dualstack.js',
    tinted: true,
    sources: [
      { label: 'IPv4/IPv6 Dual-Stack', href: 'https://kubernetes.io/docs/concepts/services-networking/dual-stack/' },
    ],
  },
  {
    id: 'network-kube-proxy-modes',
    title: 'kube-proxy: iptables vs IPVS',
    category: 'network',
    subcategory: 'foundations',
    desc: 'How does kube-proxy actually pick a backend, and why does the mode matter at scale? In iptables mode a packet to a ClusterIP walks KUBE-SERVICES into a per-Service chain that picks an endpoint by probability, then a per-endpoint chain DNATs it to the Pod. IPVS mode does the same selection with an in-kernel hash table and real schedulers, trading the sequential rule walk for constant-time lookups once a cluster has thousands of Services.',
    k8sVersion: '1.35',
    module: './schemes/network-kube-proxy-modes.js',
    tinted: true,
    sources: [
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
    ],
  },
  {
    id: 'network-netfilter-path',
    title: 'The Netfilter Path a Packet Takes',
    category: 'network',
    subcategory: 'foundations',
    desc: 'The DNAT kube-proxy programs, the conntrack entry that pins a flow and the MASQUERADE on egress all run at fixed netfilter hooks, and the order of those hooks is what makes them work. A packet enters at PREROUTING, where conntrack records the flow and the nat table rewrites the Service address to a backend Pod. Only then does routing run, on the already rewritten address, which is precisely why DNAT must come first. The reply is untangled by conntrack with no rule walk at all.',
    k8sVersion: '1.35',
    module: './schemes/network-netfilter-path.js',
    tinted: true,
    sources: [
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
      { label: 'netfilter packet flow', href: 'https://wiki.nftables.org/wiki-nftables/index.php/Netfilter_hooks' },
    ],
  },
  {
    id: 'network-ebpf-dataplane',
    title: 'eBPF Dataplane',
    category: 'network',
    subcategory: 'foundations',
    desc: 'How does the modern dataplane route Services without iptables? The CNI agent attaches eBPF programs to kernel hooks and keeps Service and endpoint state in BPF maps. A connection to a ClusterIP is then looked up and rewritten to a backend right at the socket: connect-time load balancing instead of per-packet DNAT, with constant-time lookups that let kube-proxy and its iptables chains be removed entirely.',
    k8sVersion: '1.35',
    module: './schemes/network-ebpf-dataplane.js',
    tinted: true,
    sources: [
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
      { label: 'eBPF', href: 'https://ebpf.io/what-is-ebpf/' },
    ],
  },
  {
    id: 'network-conntrack-nat',
    title: 'Connection Tracking and NAT',
    category: 'network',
    subcategory: 'foundations',
    desc: 'A connection is more than one packet, so NAT has to rewrite the first packet and remember that choice. The first packet of a new flow creates a conntrack entry recording the original and translated tuples and is DNAT-ed to a backend, after which replies are reverse-translated automatically with no rule walk. Every later packet matches the ESTABLISHED entry, which is why a flow sticks to one backend and why a busy Node can exhaust its conntrack table.',
    k8sVersion: '1.35',
    module: './schemes/network-conntrack-nat.js',
    tinted: true,
    sources: [
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
    ],
  },
  {
    id: 'network-pod-localhost',
    title: 'Containers Share Localhost',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'Why can an app and its sidecar reach each other over 127.0.0.1 with no network in between? Every container in a Pod joins the same network namespace, so they share one loopback, one eth0 and one Pod IP. Calls between them go through the loopback interface with no veth hop, and because the containers share one port space two of them cannot bind the same port. Outside traffic still arrives on the single eth0, so from the network a Pod looks like one host no matter how many containers run inside.',
    k8sVersion: '1.35',
    module: './schemes/network-pod-localhost.js',
    tinted: true,
    sources: [
      { label: 'Pods', href: 'https://kubernetes.io/docs/concepts/workloads/pods/' },
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
    ],
  },
  // Pod Networking runs deep to general: whose namespace the containers are in (their own, then the two
  // ways of giving that up), then how the wiring is built (the CIDR each Node draws from, the CNI call, the
  // veth it lands), and only then the traffic that rides it (same Node, across Nodes, out to the internet).
  {
    id: 'network-hostnetwork-hostport',
    title: 'hostNetwork and hostPort',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'A normal Pod has its own namespace, its own IP and a veth into the bridge. These are the two sanctioned ways to give that up. With hostNetwork true the Pod gets no namespace at all: no veth, no Pod IP, it binds the Node address directly, which is how kube-proxy and the CNI agent run. A hostPort leaves the Pod intact and only adds a DNAT rule from the CNI portmap plugin, mapping Node address and host port to Pod IP and container port. Both spend a Node port, so the scheduler fits only one such replica per Node.',
    k8sVersion: '1.35',
    module: './schemes/network-hostnetwork-hostport.js',
    tinted: true,
    sources: [
      { label: 'Pods and host namespaces', href: 'https://kubernetes.io/docs/concepts/workloads/pods/#pod-networking' },
      { label: 'Do not use hostPort or hostNetwork lightly', href: 'https://kubernetes.io/docs/concepts/configuration/overview/#services' },
      { label: 'CNI portmap plugin', href: 'https://www.cni.dev/plugins/current/meta/portmap/' },
    ],
  },
  {
    id: 'network-ipam-pod-cidr',
    title: 'IPAM and Pod CIDR Allocation',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'How does every Pod in the cluster end up with a unique IP and no clashes? The cluster starts from one large pod CIDR, and the controller-manager carves a smaller non-overlapping slice out of it for each Node into node.spec.podCIDR. The CNI IPAM on each Node then hands out Pod IPs only from its own slice, which keeps addresses unique cluster-wide and routing simple.',
    k8sVersion: '1.35',
    module: './schemes/network-ipam-pod-cidr.js',
    tinted: true,
    sources: [
      { label: 'kube-controller-manager', href: 'https://kubernetes.io/docs/reference/command-line-tools-reference/kube-controller-manager/' },
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
    ],
  },
  {
    id: 'network-cni-invocation',
    title: 'Wiring a Pod via CNI',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'Who actually plumbs a Pod onto the network? The kubelet asks the CRI runtime to create the Pod sandbox, a pause container holding a fresh network namespace with loopback only. The runtime then invokes the CNI ADD operation, and the plugin chain creates a veth pair, attaches it to the host bridge and gets an IP from an IPAM plugin. That address, plus routes and DNS, is written into the namespace as eth0.',
    k8sVersion: '1.35',
    module: './schemes/network-cni-invocation.js',
    tinted: true,
    sources: [
      { label: 'CNI Specification', href: 'https://github.com/containernetworking/cni/blob/main/SPEC.md' },
      { label: 'Network Plugins', href: 'https://kubernetes.io/docs/concepts/extend-kubernetes/compute-storage-net/network-plugins/' },
    ],
  },
  {
    id: 'network-pod-ip-and-veth',
    title: 'Pod IP and the veth Pair',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'Where does a Pod get its IP, and how is it wired to the Node? The pause container opens one network namespace that every container in the Pod is joined into, so they share a single address and reach each other over localhost. When the sandbox starts the CNI plugin draws one Pod IP from its IPAM and builds a veth pair, moving one end into the namespace as eth0 and attaching the peer to the cni0 host bridge.',
    k8sVersion: '1.35',
    module: './schemes/network-pod-ip-and-veth.js',
    tinted: true,
    sources: [
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
      { label: 'CNI Specification', href: 'https://github.com/containernetworking/cni/blob/main/SPEC.md' },
    ],
  },
  {
    id: 'network-pod-to-pod-same-node',
    title: 'Pod-to-Pod on the Same Node',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'How does a packet get from one Pod to another when both run on the same Node? The two Pods share the Node podCIDR subnet, so the source simply sends to the destination Pod IP and the frame crosses a veth pair onto the cni0 bridge. The bridge switches it straight out the peer veth at layer 2, with no NAT and no encapsulation, so the real source IP arrives intact.',
    k8sVersion: '1.35',
    module: './schemes/network-pod-to-pod-same-node.js',
    tinted: true,
    sources: [
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
    ],
  },
  {
    id: 'network-pod-to-pod-cross-node',
    title: 'Pod-to-Pod Across Nodes',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'What happens when the destination Pod lives on a different Node? The local bridge has no port for that Pod, so the CNI takes over the inter-Node hop. An overlay plugin wraps the frame in VXLAN over UDP to the remote Node IP, whose kernel decapsulates it and delivers it through its own bridge, while a routed plugin instead advertises each Node podCIDR so the frame travels with no encapsulation at all.',
    k8sVersion: '1.35',
    module: './schemes/network-pod-to-pod-cross-node.js',
    tinted: true,
    sources: [
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
    ],
  },
  {
    id: 'network-pod-egress-snat',
    title: 'Pod Egress to the Internet',
    category: 'network',
    subcategory: 'pod-networking',
    desc: 'A Pod IP is private to the cluster, so how does a Pod reach the internet and get a reply back? As the packet leaves the Node an iptables MASQUERADE rule rewrites its source from the Pod IP to the Node IP, and conntrack records the mapping. The server replies to the Node, conntrack reverses the translation, and the reply lands back on the Pod, which only ever used its own address.',
    k8sVersion: '1.35',
    module: './schemes/network-pod-egress-snat.js',
    tinted: true,
    sources: [
      { label: 'Cluster Networking', href: 'https://kubernetes.io/docs/concepts/cluster-administration/networking/' },
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
    ],
  },
  // Services & Endpoints opens on the map (which type does what), then the ClusterIP dataplane, then the
  // endpoint set that dataplane is programmed from, how a backend is chosen out of it, the ports it is
  // reached on, what happens as one leaves, and finally the two Services that skip the proxy entirely.
  {
    id: 'network-service-types',
    title: 'Service Types and How They Stack',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'Kubernetes has five Service types, so how do they relate? Three of them stack: ClusterIP is the internal base with a virtual IP, NodePort builds on it by opening the same port on every Node, and LoadBalancer builds on NodePort with a cloud-provisioned external address. The other two skip the proxy entirely and work through DNS alone, ExternalName returning a CNAME to an external host and Headless returning the backing Pod IPs directly.',
    k8sVersion: '1.35',
    module: './schemes/network-service-types.js',
    tinted: true,
    sources: [
      { label: 'Service', href: 'https://kubernetes.io/docs/concepts/services-networking/service/' },
      { label: 'Publishing Services', href: 'https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types' },
    ],
  },
  {
    id: 'network-service-clusterip',
    title: 'ClusterIP via kube-proxy',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'A ClusterIP belongs to no network interface, so what makes traffic to it actually reach a Pod? kube-proxy watches the Service and its EndpointSlices and programs the Node dataplane so that any packet sent to the ClusterIP is DNAT-ed to one of the backing Pod IPs. Connection tracking remembers each choice, so every connection sticks to the backend it first landed on while the reply is rewritten back to the ClusterIP, the only address the client ever sees.',
    k8sVersion: '1.35',
    module: './schemes/network-service-clusterip.js',
    tinted: true,
    sources: [
      { label: 'Service', href: 'https://kubernetes.io/docs/concepts/services-networking/service/' },
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
    ],
  },
  {
    id: 'network-endpointslice-reconcile',
    title: 'Service and EndpointSlice',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'A Service is a stable name for a moving set of Pods, so how does it learn which Pods back it right now? The EndpointSlice controller watches the Pods matching the Service selector and records each as an IP and port endpoint in EndpointSlice objects. Readiness gates the serving set, not liveness, so a Pod failing its readiness probe is flagged notReady and excluded without a restart, and kube-proxy reprograms its dataplane from those slices.',
    k8sVersion: '1.35',
    module: './schemes/network-endpointslice-reconcile.js',
    tinted: true,
    sources: [
      { label: 'Service', href: 'https://kubernetes.io/docs/concepts/services-networking/service/' },
      { label: 'EndpointSlices', href: 'https://kubernetes.io/docs/concepts/services-networking/endpoint-slices/' },
    ],
  },
  {
    id: 'network-traffic-distribution',
    title: 'Session Affinity and Topology',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'How does kube-proxy choose which backend gets a connection, and can you steer it? By default it spreads connections evenly across every ready endpoint and ignores zones. Two fields tune that: sessionAffinity ClientIP pins each client source IP to one Pod so a session stays put, and trafficDistribution PreferSameZone favors endpoints in the same zone as the client to cut latency and cross-zone charges, with a fallback to other zones when the local one has none.',
    k8sVersion: '1.35',
    module: './schemes/network-traffic-distribution.js',
    tinted: true,
    sources: [
      { label: 'Topology Aware Routing', href: 'https://kubernetes.io/docs/concepts/services-networking/topology-aware-routing/' },
      { label: 'Service Session Affinity', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/#session-affinity' },
    ],
  },
  {
    id: 'network-internal-traffic-policy',
    title: 'Internal Traffic Cluster vs Local',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'internalTrafficPolicy is the east-west twin of externalTrafficPolicy: the same two values, but for callers inside the cluster. With Cluster the kube-proxy on the client Node programs every ready endpoint, so a call to the ClusterIP can be DNAT-ed to a backend on another Node and pay a cross-node hop. With Local it keeps only the endpoints on that same Node, which is how a Pod reaches the node-local agent of a DaemonSet. There is no fallback though, so a Node with no local backend drops the traffic.',
    k8sVersion: '1.35',
    module: './schemes/network-internal-traffic-policy.js',
    tinted: true,
    sources: [
      { label: 'Service Internal Traffic Policy', href: 'https://kubernetes.io/docs/concepts/services-networking/service-traffic-policy/' },
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
    ],
  },
  {
    id: 'network-service-ports',
    title: 'Service Port Remapping',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'A Service has two port numbers that are easy to mix up, so which does what? The port is the stable front door that clients dial, while the targetPort is the port the container actually listens on. They do not have to match, because kube-proxy rewrites the destination port as it DNATs the packet to a backend Pod. The targetPort can even be a name like http that each Pod resolves to its own containerPort, so the container port can move while every client keeps dialing the same Service port.',
    k8sVersion: '1.35',
    module: './schemes/network-service-ports.js',
    tinted: true,
    sources: [
      { label: 'Service', href: 'https://kubernetes.io/docs/concepts/services-networking/service/' },
      { label: 'Defining a Service', href: 'https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service' },
    ],
  },
  {
    id: 'network-service-terminating-endpoints',
    title: 'Terminating Endpoints and Draining',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'When a rolling update deletes a backing Pod, why do requests keep flowing instead of failing? The Pod enters Terminating and the EndpointSlice controller flips its endpoint to notReady while serving and terminating stay true, so kube-proxy stops sending new connections to it but keeps draining the in-flight ones through the terminationGracePeriodSeconds window. Only after the grace period does the endpoint leave the slice, so a clean rollout drops nothing.',
    k8sVersion: '1.35',
    module: './schemes/network-service-terminating-endpoints.js',
    tinted: true,
    sources: [
      { label: 'EndpointSlices', href: 'https://kubernetes.io/docs/concepts/services-networking/endpoint-slices/' },
      { label: 'Pod Termination', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-termination' },
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
    ],
  },
  {
    id: 'network-externalname',
    title: 'ExternalName and Services without Selectors',
    category: 'network',
    subcategory: 'service-networking',
    desc: 'Most Services proxy to Pods chosen by a selector, so how do the two kinds that skip selectors work? A type ExternalName Service has no ClusterIP at all, CoreDNS just returns a CNAME to an external name and the client connects there directly, with kube-proxy never involved. A ClusterIP Service with no selector keeps a real virtual IP but gets no automatic endpoints, so you attach an EndpointSlice by hand listing an external IP and kube-proxy DNATs to it like any other Service. Same Service object, two very different jobs.',
    k8sVersion: '1.35',
    module: './schemes/network-externalname.js',
    tinted: true,
    sources: [
      { label: 'ExternalName Services', href: 'https://kubernetes.io/docs/concepts/services-networking/service/#externalname' },
      { label: 'Services without selectors', href: 'https://kubernetes.io/docs/concepts/services-networking/service/#services-without-selectors' },
    ],
  },
  {
    id: 'network-dns-coredns',
    title: 'DNS Resolution via CoreDNS',
    category: 'network',
    subcategory: 'dns',
    desc: 'When a Pod connects to a Service by name, who turns that name into an IP? The Kubelet writes a resolv.conf whose nameserver is the kube-dns Service ClusterIP, with search domains and ndots that expand a short name like web into web.default.svc.cluster.local. Because kube-dns is itself a Service, the query is load balanced to one of the CoreDNS Pods, where the plugin chain tries cache first and then the kubernetes plugin, which answers the cluster zone from its running watch of Services and Endpoints rather than querying the Api per lookup. Back comes an A record holding the Service ClusterIP, and only then can the connection open.',
    k8sVersion: '1.35',
    module: './schemes/network-dns-coredns.js',
    tinted: true,
    sources: [
      { label: 'DNS for Services and Pods', href: 'https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/' },
    ],
  },
  {
    id: 'network-dns-records',
    title: 'DNS Records and FQDN',
    category: 'network',
    subcategory: 'dns',
    desc: 'A Service name is really a fully qualified name, web.default.svc.cluster.local, built from the Service, its namespace, the literal svc and the cluster domain. The kind segment in the middle is what decides the shape of the answer. The same name comes back as an A record holding the ClusterIP, as an SRV record that publishes a named port, or, for a headless Service, as one A record per ready Pod. Pods themselves are addressed under the pod kind, where the dashed form of the Pod IP resolves to that one Pod.',
    k8sVersion: '1.35',
    module: './schemes/network-dns-records.js',
    tinted: true,
    sources: [
      { label: 'DNS for Services and Pods', href: 'https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/' },
    ],
  },
  {
    id: 'network-dns-ndots',
    title: 'Search Domains and ndots',
    category: 'network',
    subcategory: 'dns',
    desc: 'Every Pod gets a resolv.conf listing search domains and an ndots value, and together they decide what a short name costs to resolve. A name with fewer than ndots dots counts as relative, so the resolver appends each search domain in turn and only tries the name as written once the list runs out. A name that exists is answered on the first candidate, but one that does not exist walks all four of them, and because the resolver asks for IPv4 and IPv6 in parallel a single failed lookup can put eight queries on the wire. Ending a name with a trailing dot makes it absolute, which skips the search list entirely and is the usual fix for noisy cluster DNS.',
    k8sVersion: '1.35',
    module: './schemes/network-dns-ndots.js',
    tinted: true,
    sources: [
      { label: 'DNS for Services and Pods', href: 'https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/' },
      { label: 'Pod DNS Config', href: 'https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/#pod-dns-config' },
    ],
  },
  {
    id: 'network-nodelocal-dnscache',
    title: 'NodeLocal DNSCache',
    category: 'network',
    subcategory: 'dns',
    desc: 'Without a local cache, every Pod DNS query is a UDP packet that kube-proxy DNATs across the cluster to a CoreDNS Pod, adding latency and a conntrack entry per lookup, and the race on those short UDP flows is what surfaces as the notorious five second lookup stalls. NodeLocal DNSCache runs as a DaemonSet, so a DNS agent sits on every Node on a link-local address that Pods query. A cached name is answered right there with no cluster hop, no DNAT and no conntrack entry, and only a miss is forwarded upstream to CoreDNS over one long-lived TCP connection instead of a fresh UDP flow. On busy clusters this is one of the simplest ways to keep DNS from becoming the bottleneck.',
    k8sVersion: '1.35',
    module: './schemes/network-nodelocal-dnscache.js',
    tinted: true,
    sources: [
      { label: 'NodeLocal DNSCache', href: 'https://kubernetes.io/docs/tasks/administer-cluster/nodelocaldns/' },
      { label: 'DNS for Services and Pods', href: 'https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/' },
    ],
  },
  {
    id: 'network-headless-service',
    title: 'Headless Service',
    category: 'network',
    subcategory: 'dns',
    desc: 'A Service set with clusterIP None is headless: the cluster gives it no virtual IP, so kube-proxy programs nothing. Instead CoreDNS reads the ready endpoints and returns one A record per backing Pod, and the client connects to a Pod IP directly with no DNAT in the path. The catch is that nothing is balancing that traffic any more, the client picks an address out of the set itself, and one that keeps the first record it was given will pin itself to a single Pod. A headless Service also gives each StatefulSet Pod its own stable name, so web-0.web.default.svc.cluster.local always resolves to that exact replica, which is how a stateful system addresses a known primary.',
    k8sVersion: '1.35',
    module: './schemes/network-headless-service.js',
    tinted: true,
    sources: [
      { label: 'Headless Services', href: 'https://kubernetes.io/docs/concepts/services-networking/service/#headless-services' },
      { label: 'DNS for Services and Pods', href: 'https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/' },
    ],
  },
  // External Traffic runs map first, then layer 4, then layer 7: the north-south path is the overview
  // every later card zooms into, NodePort and LoadBalancer is how the address exists, bare metal is how
  // it exists without a cloud, externalTrafficPolicy and client-IP preservation are the two things that
  // path does to the source address, and Ingress, TLS and Gateway API are the layer 7 edge on top.
  {
    id: 'network-north-south-path',
    title: 'North-South Request Path',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'A request from the public internet does not reach a Pod in one jump, it crosses a chain of hops that a LoadBalancer Service sets up. The client connects to the public IP of a cloud load balancer, which forwards to a Node on the service NodePort. kube-proxy on that Node DNATs the packet to a backing Pod IP and conntrack pins the flow, and the Pod serves the request. The reply retraces the chain in reverse so the client sees a response that appears to come straight from the public IP it dialed.',
    k8sVersion: '1.35',
    module: './schemes/network-north-south-path.js',
    tinted: true,
    sources: [
      { label: 'Publishing Services', href: 'https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types' },
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
    ],
  },
  {
    id: 'network-nodeport-loadbalancer',
    title: 'NodePort and LoadBalancer',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'How does traffic from outside the cluster reach a Pod? A NodePort opens the same high port on every Node, so a packet to any Node on that port is DNAT-ed to a backing Pod, even one on another Node. A LoadBalancer builds on top: the cloud-controller-manager provisions an external load balancer whose targets are those node ports, giving clients a single stable address in front of the cluster.',
    k8sVersion: '1.35',
    module: './schemes/network-nodeport-loadbalancer.js',
    tinted: true,
    sources: [
      { label: 'Service', href: 'https://kubernetes.io/docs/concepts/services-networking/service/' },
      { label: 'Virtual IPs and Service Proxies', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/' },
    ],
  },
  {
    id: 'network-loadbalancer-bare-metal',
    title: 'LoadBalancer without a Cloud',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'Who provisions the load balancer when there is no cloud? Nobody, so the Service stays pending forever. An in-cluster implementation such as MetalLB fills the gap: it allocates an address from a pool the operator declares and then has to announce it. In layer 2 mode one elected Node answers ARP for the address and takes all inbound traffic, with a gratuitous ARP on failover. In BGP mode every Node advertises the address and the router hashes flows across all of them.',
    k8sVersion: '1.35',
    module: './schemes/network-loadbalancer-bare-metal.js',
    tinted: true,
    sources: [
      { label: 'Service type LoadBalancer', href: 'https://kubernetes.io/docs/concepts/services-networking/service/#loadbalancer' },
      { label: 'MetalLB Concepts', href: 'https://metallb.io/concepts/' },
    ],
  },
  {
    id: 'network-externaltrafficpolicy',
    title: 'External Traffic Cluster vs Local',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'When external traffic lands on a Node, should it spread evenly or keep the client IP? With externalTrafficPolicy Cluster the packet can be forwarded to a backend on any Node, which balances load but can add a hop and SNATs away the client source IP. With Local the Node only serves its own local Pods, which preserves the client IP and removes the extra hop, at the cost of dropping traffic on Nodes that have no local backend, so a health check steers the balancer around them.',
    k8sVersion: '1.35',
    module: './schemes/network-externaltrafficpolicy.js',
    tinted: true,
    sources: [
      { label: 'ExternalTrafficPolicy', href: 'https://kubernetes.io/docs/reference/networking/virtual-ips/#external-traffic-policy' },
    ],
  },
  {
    id: 'network-client-ip-preservation',
    title: 'Preserving the Client IP',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'An edge proxy terminates the client connection and opens a new one to the backend from its own Pod address, so the socket the app reads no longer carries the client at all. For HTTP the edge writes the original address into X-Forwarded-For and the standard Forwarded header, which a client can forge, so only the value your own trusted edge wrote may be believed. For raw TCP or TLS passthrough there is no header to write into, and the PROXY protocol prepends a preamble carrying the original addresses instead.',
    k8sVersion: '1.35',
    module: './schemes/network-client-ip-preservation.js',
    tinted: true,
    sources: [
      { label: 'Preserving the client source IP', href: 'https://kubernetes.io/docs/tutorials/services/source-ip/' },
      { label: 'Forwarded header (RFC 7239)', href: 'https://www.rfc-editor.org/rfc/rfc7239' },
    ],
  },
  {
    id: 'network-ingress-routing',
    title: 'Ingress Controller Routing',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'An Ingress object is just a set of host and path rules, so what actually serves the traffic? An Ingress controller Pod watches those objects and turns them into live proxy configuration. External traffic reaches it through a Service of its own, where TLS is terminated. The controller then matches the request Host and path against the rules, and the longest match wins, so it proxies straight to a Ready Pod IP read from the EndpointSlice.',
    k8sVersion: '1.35',
    module: './schemes/network-ingress-routing.js',
    tinted: true,
    sources: [
      { label: 'Ingress', href: 'https://kubernetes.io/docs/concepts/services-networking/ingress/' },
      { label: 'Ingress Controllers', href: 'https://kubernetes.io/docs/concepts/services-networking/ingress-controllers/' },
    ],
  },
  {
    id: 'network-tls-termination',
    title: 'TLS Termination at the Edge',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'Clients reach the cluster over HTTPS, but backend Pods usually speak plain HTTP, so the edge handles the encryption. The Ingress or Gateway presents a certificate from a Kubernetes Secret, completes the handshake, decrypts the request, and proxies plain HTTP to a Pod that never sees a certificate. Re-encrypt keeps traffic encrypted to the backend, and passthrough lets the Pod terminate TLS itself.',
    k8sVersion: '1.35',
    module: './schemes/network-tls-termination.js',
    tinted: true,
    sources: [
      { label: 'Ingress TLS', href: 'https://kubernetes.io/docs/concepts/services-networking/ingress/#tls' },
      { label: 'Gateway API', href: 'https://gateway-api.sigs.k8s.io/' },
    ],
  },
  {
    id: 'network-gateway-api',
    title: 'Gateway API',
    category: 'network',
    subcategory: 'external-traffic',
    desc: 'How does the Gateway API split ingress across the teams that own its parts? A GatewayClass names the controller implementation, a Gateway declares the listeners and is owned by cluster operators, and an HTTPRoute attaches to that Gateway with the host and path rules an application team controls. A request flows from a Gateway listener through the matched HTTPRoute rule to a backend Service, making this the role-oriented, now stable successor to Ingress.',
    k8sVersion: '1.35',
    module: './schemes/network-gateway-api.js',
    tinted: true,
    sources: [
      { label: 'Gateway API', href: 'https://kubernetes.io/docs/concepts/services-networking/gateway/' },
      { label: 'Gateway API Project', href: 'https://gateway-api.sigs.k8s.io/' },
    ],
  },
  {
    id: 'workloads-rolling-update',
    title: 'Deployment Rolling Update',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'How do you move every Pod to a new version without taking the app offline? A Deployment rolls the change out gradually, bringing up Pods on the new version a few at a time and retiring the old ones only once the replacements are healthy, so the Service always keeps enough backends serving traffic. If the new version misbehaves, the same machinery rolls it back just as smoothly.',
    k8sVersion: '1.35',
    module: './schemes/workloads-rolling-update.js',
    tinted: true,
    sources: [
      { label: 'Deployments', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/deployment/' },
    ],
  },
  {
    id: 'workloads-deployment-rollback',
    title: 'Deployment Rollback and Revision History',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'A rollout went bad, how do you get back to the version that worked? A Deployment keeps its old ReplicaSets around as numbered revisions, so a rollback is just scaling the previous one back up while the broken one is scaled to zero. The same revision history is how a stuck rollout is detected and undone in a single command.',
    k8sVersion: '1.35',
    module: './schemes/workloads-deployment-rollback.js',
    tinted: true,
    sources: [
      { label: 'Rolling Back a Deployment', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#rolling-back-a-deployment' },
    ],
  },
  // Volume Foundations teaches what a volume even is before any persistence machinery: it belongs to
  // the Pod, the container filesystem underneath is throwaway, emptyDir lives and dies with the Pod.
  // ConfigMap/Secret and projected mounts and the ephemeral-storage limits round out the local model,
  // then ephemeral-vs-persistent is the bridge that hands off to Volumes and Claims.
  {
    id: 'storage-volume-model',
    title: 'Pod Volume Model',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'What does it actually mean for a Pod to have a volume, and who does it belong to? A volume is declared once at the Pod level under spec.volumes and mounted into each container at volumeMounts, possibly at a different path. Because it belongs to the Pod and not to any container, two containers that mount it share every write and the data survives a container crashing and restarting. An ephemeral volume like this lives and dies with the Pod: delete the Pod and the volume goes with it, which is exactly the gap persistent storage exists to close.',
    k8sVersion: '1.35',
    module: './schemes/storage-volume-model.js',
    tinted: true,
    sources: [
      { label: 'Volumes', href: 'https://kubernetes.io/docs/concepts/storage/volumes/' },
      { label: 'Volumes Background', href: 'https://kubernetes.io/docs/concepts/storage/volumes/#background' },
    ],
  },
  {
    id: 'storage-container-filesystem',
    title: 'Container Filesystem Layers',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'Why does a file written inside a container vanish when it restarts, but a file on a volume does not? A container root filesystem is a stack of read-only image layers with one thin writable layer on top, merged by overlayfs. Every change lands in that writable layer rather than touching the image, and the writable layer is thrown away when the container is removed. A mounted volume is a hole punched straight through the overlay to real storage, which is the only reason its data outlives the container.',
    k8sVersion: '1.35',
    module: './schemes/storage-container-filesystem.js',
    tinted: true,
    sources: [
      { label: 'Volumes Background', href: 'https://kubernetes.io/docs/concepts/storage/volumes/#background' },
      { label: 'Images', href: 'https://kubernetes.io/docs/concepts/containers/images/' },
    ],
  },
  {
    id: 'storage-emptydir',
    title: 'emptyDir Lifecycle',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'Where does an emptyDir live, and exactly when does it disappear? It is created empty the moment the Pod is assigned to a node, sits on that node disk, and is shared by every container in the Pod. It survives a container crashing and restarting but is deleted forever when the Pod is removed from the node. Set medium to Memory and it becomes a tmpfs that counts against the memory limit, while a sizeLimit that is exceeded gets the Pod evicted.',
    k8sVersion: '1.35',
    module: './schemes/storage-emptydir.js',
    tinted: true,
    sources: [
      { label: 'emptyDir', href: 'https://kubernetes.io/docs/concepts/storage/volumes/#emptydir' },
      { label: 'Ephemeral Volumes', href: 'https://kubernetes.io/docs/concepts/storage/ephemeral-volumes/' },
    ],
  },
  {
    id: 'storage-hostpath',
    title: 'hostPath Volumes',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'Why does a hostPath look like persistent storage yet quietly is not? It mounts a file or directory that already exists on the node straight into the Pod, so unlike an emptyDir the kubelet never creates it. The directory belongs to the node, not the Pod, so its data stays behind when the Pod is gone and does not follow a reschedule onto another node. Pointed at the runtime socket or host root it hands over the whole node, which is why the Baseline and Restricted Pod Security Standards forbid it outside node agents.',
    k8sVersion: '1.35',
    module: './schemes/storage-hostpath.js',
    tinted: true,
    sources: [
      { label: 'hostPath', href: 'https://kubernetes.io/docs/concepts/storage/volumes/#hostpath' },
      { label: 'Pod Security Standards', href: 'https://kubernetes.io/docs/concepts/security/pod-security-standards/' },
    ],
  },
  {
    id: 'storage-configmap-secret-mount',
    title: 'ConfigMap and Secret as Files',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'How does a ConfigMap or Secret become files, and why does an update never show a half-written config? Each key becomes a file, and kubelet writes them into a timestamped directory and flips a single ..data symlink so every update is atomic. Changes arrive on the kubelet sync period, up to about a minute, and the app must re-read the file itself because nothing restarts it. A subPath mount opts out of the symlink swap and never updates, and a Secret defaults to tmpfs so its files never touch the node disk.',
    k8sVersion: '1.35',
    module: './schemes/storage-configmap-secret-mount.js',
    tinted: true,
    sources: [
      { label: 'ConfigMaps', href: 'https://kubernetes.io/docs/concepts/configuration/configmap/' },
      { label: 'Secrets', href: 'https://kubernetes.io/docs/concepts/configuration/secret/' },
    ],
  },
  {
    id: 'storage-projected-volume',
    title: 'Projected Volumes',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'What if one directory needs to combine a ConfigMap, a Secret, Pod metadata and an API token at once? A projected volume assembles all of them into a single mount, with the serviceAccountToken source being the one that matters. Unlike the old Secret-based token that was valid forever, a projected token is short-lived and bound to an audience, and kubelet rotates it in place before it expires. The app just keeps reading the same file and always finds a valid, scoped token.',
    k8sVersion: '1.35',
    module: './schemes/storage-projected-volume.js',
    tinted: true,
    sources: [
      { label: 'Projected Volumes', href: 'https://kubernetes.io/docs/concepts/storage/projected-volumes/' },
      { label: 'Service Accounts', href: 'https://kubernetes.io/docs/concepts/security/service-accounts/' },
    ],
  },
  {
    id: 'storage-ephemeral-storage-eviction',
    title: 'Ephemeral Storage Limits',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'A container fills the node disk with logs and temp files, so which Pod gets evicted and why? There are two separate paths. A per-Pod limit, limits.ephemeral-storage, evicts that one Pod the instant its writable layer plus emptyDir plus logs exceed it, no matter how healthy the node is. Node-pressure eviction is the other: when the node filesystem crosses its threshold kubelet taints the node DiskPressure and evicts the Pods that are over their ephemeral request, ranked by Pod Priority and by how far each is over that request, which can take out a Pod that was within its own limit.',
    k8sVersion: '1.35',
    module: './schemes/storage-ephemeral-storage-eviction.js',
    tinted: true,
    sources: [
      { label: 'Local Ephemeral Storage', href: 'https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#local-ephemeral-storage' },
      { label: 'Node-pressure Eviction', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/node-pressure-eviction/' },
    ],
  },
  {
    id: 'storage-ephemeral-vs-persistent',
    title: 'Ephemeral vs Persistent',
    category: 'storage',
    subcategory: 'foundations',
    desc: 'Two volumes in one Pod, both written to, so why does only one survive a reschedule? An emptyDir is tied to the node, so when the Pod is deleted and lands on another node it comes back completely empty. A PersistentVolumeClaim is tied to nothing but the claim, so it reattaches the very same disk with the data intact. Same Pod spec, two outcomes: put throwaway scratch in an emptyDir and anything you must keep behind a PVC.',
    k8sVersion: '1.35',
    module: './schemes/storage-ephemeral-vs-persistent.js',
    tinted: true,
    sources: [
      { label: 'Persistent Volumes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
      { label: 'Ephemeral Volumes', href: 'https://kubernetes.io/docs/concepts/storage/ephemeral-volumes/' },
    ],
  },
  // Volumes and Claims follows one claim through its whole life, in the order the events actually
  // happen. A claim is only a statement of need, so binding is what turns it into real storage and
  // dynamic provisioning is what happens when there is nothing to bind to. Access modes and
  // volumeMode are the two spec fields that decide what the workload is handed, expansion is the one
  // thing you can change while the volume is live, and the last three are the deletion arc in event
  // order: the finalizer holds the claim, the reclaim policy decides the ending, and the phase
  // machine is the map of all of it. Everything in the categories after this one (how the volume is
  // attached, where the bytes land, how a StatefulSet gets one per replica) assumes this pairing.
  {
    id: 'storage-pvc-binding',
    title: 'PVC to PV Binding',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'A PersistentVolumeClaim is a request, not storage: it says only how much space, which access mode and which class the workload needs. So who turns that request into a real disk? The binding controller scans the volumes that are Available, throws out the ones that are too small or of the wrong class, and pairs the claim with one that fits by writing the link on both objects at once. That pairing is exclusive and permanent, which is why a second claim asking for the very same volume just waits, and keeps waiting until a volume it can actually have turns up.',
    k8sVersion: '1.35',
    module: './schemes/storage-pvc-binding.js',
    tinted: true,
    sources: [
      { label: 'Persistent Volumes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
      { label: 'Binding', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#binding' },
    ],
  },
  {
    id: 'storage-dynamic-provisioning',
    title: 'StorageClass Provisioning',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'Binding only works if a matching volume already exists, so what happens when none does and no administrator is around to create one? The claim names a StorageClass, and that class names a provisioner. The provisioner watches for Pending claims pointing at its class, calls CreateVolume on the driver to have a real disk carved out of the storage backend, then writes a PersistentVolume object to represent it. The volume is built to order and bound immediately, because it was made for that one claim and carries its claimRef from birth.',
    k8sVersion: '1.35',
    module: './schemes/storage-dynamic-provisioning.js',
    tinted: true,
    sources: [
      { label: 'Dynamic Provisioning', href: 'https://kubernetes.io/docs/concepts/storage/dynamic-provisioning/' },
      { label: 'Storage Classes', href: 'https://kubernetes.io/docs/concepts/storage/storage-classes/' },
    ],
  },
  {
    id: 'storage-access-modes',
    title: 'Access Modes',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'ReadWriteOnce sounds like one Pod, so why can two Pods share the same volume? Because the mode is per node, not per Pod: once a block disk is attached to a node, any Pod scheduled there can mount it, while a Pod on another node gets a Multi-Attach error. ReadWriteOncePod is the strict version that really does mean one Pod, and ReadWriteMany needs a shared filesystem like NFS or CephFS, since a raw block disk cannot attach to many nodes at all. Mostly the mode is a request the CSI driver has to honour rather than a rule Kubernetes enforces, with ReadWriteOncePod the one exception it polices itself.',
    k8sVersion: '1.35',
    module: './schemes/storage-access-modes.js',
    tinted: true,
    sources: [
      { label: 'Access Modes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes' },
      { label: 'Persistent Volumes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
    ],
  },
  {
    id: 'storage-volume-mode',
    title: 'Filesystem vs Block',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'Two claims for the same disk, one field apart, so why does only one come back with a filesystem? volumeMode is Filesystem unless you say otherwise: the CSI node service runs mkfs on a blank device, mounts it, and the container finds an ordinary directory at the mountPath under volumeMounts. Set it to Block and none of that happens. The raw device is published straight into the container at a devicePath under volumeDevices, so fsGroup, subPath and file permissions all stop applying and the application owns everything above the first byte. The field is immutable and must match on both objects.',
    k8sVersion: '1.35',
    module: './schemes/storage-volume-mode.js',
    tinted: true,
    sources: [
      { label: 'Volume Mode', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#volume-mode' },
      { label: 'Raw Block Volume Support', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#raw-block-volume-support' },
    ],
  },
  {
    id: 'storage-volume-expansion',
    title: 'Online Volume Expansion',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'Bumping a PVC to a bigger size does not grow anything on its own, so what actually makes a running Pod see more space? The Api accepts that edit only if allowVolumeExpansion is true on the StorageClass, and then it runs in two phases: the external-resizer calls ControllerExpandVolume to grow the disk, then kubelet calls NodeExpandVolume on the node to grow the filesystem on the mounted device. The disk grows first and the filesystem second, and only after both does the extra room appear in the container, with no restart. Shrinking is refused, because there is no safe general way to make a filesystem with live data on it smaller.',
    k8sVersion: '1.35',
    module: './schemes/storage-volume-expansion.js',
    tinted: true,
    sources: [
      { label: 'Expanding Persistent Volumes Claims', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#expanding-persistent-volumes-claims' },
      { label: 'Resizing a Volume Using a Pod', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#resizing-an-existing-volume-using-a-pod' },
    ],
  },
  {
    id: 'storage-pvc-protection',
    title: 'PVC Protection Finalizers',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'You run kubectl delete pvc and the claim sits there in Terminating and refuses to go away, so what is holding it? The kubernetes.io/pvc-protection finalizer, put on every claim at creation: while a Pod still mounts it the deletionTimestamp is set but the API server will not complete the delete, because pulling storage from under a running Pod would break it. The phase stays Bound the whole time, Terminating is only what kubectl prints once a deletionTimestamp exists. Only when the last consuming Pod is gone does the controller drop the finalizer and the object disappear. The fix is almost always to find the Pod still using it.',
    k8sVersion: '1.35',
    module: './schemes/storage-pvc-protection.js',
    tinted: true,
    sources: [
      { label: 'Storage Object in Use Protection', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#storage-object-in-use-protection' },
      { label: 'Finalizers', href: 'https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/' },
    ],
  },
  {
    id: 'storage-reclaim-policy',
    title: 'Reclaim Policy',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'You delete a PVC: does the disk full of data go with it? That is decided by one field on the PV, the reclaim policy. Under Delete the controller calls DeleteVolume, wipes the backing disk and removes the PV object, so the cleanup is automatic and the data is gone for good. Under Retain nothing is touched: the PV and its disk survive, but the volume is parked in Released carrying a stale claimRef, and nothing can rebind to it until an administrator clears that reference by hand. Retain keeps your data safe at the cost of making reuse a deliberate manual step.',
    k8sVersion: '1.35',
    module: './schemes/storage-reclaim-policy.js',
    tinted: true,
    sources: [
      { label: 'Reclaiming', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#reclaiming' },
      { label: 'Retain', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#retain' },
    ],
  },
  {
    id: 'storage-pv-lifecycle-phases',
    title: 'PV Phase Machine',
    category: 'storage',
    subcategory: 'pv-pvc',
    desc: 'What does the phase of a PersistentVolume actually track, and what event moves it from one to the next? A fresh volume is Available, binding a claim flips it to Bound, and deleting that claim sends it to Released rather than back to Available, because the claimRef stays behind and goes stale. From there the reclaim policy decides the ending: Delete removes the storage asset and the PV object together, a Delete that fails leaves the volume in Failed, and Retain parks it in Released for good. Only one edge leads back and it is manual, an administrator clearing that stale claimRef by hand.',
    k8sVersion: '1.35',
    module: './schemes/storage-pv-lifecycle-phases.js',
    tinted: true,
    sources: [
      { label: 'Lifecycle of a Volume and Claim', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#lifecycle-of-a-volume-and-claim' },
      { label: 'Phase', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#phase' },
    ],
  },
  // CSI and Mount Path is the machinery: the two halves of a driver, the four-call attach and mount
  // chain, the VolumeAttachment object that records the attach, the literal path the bytes take on a
  // node, the fsGroup ownership walk at mount time, then the three ways the attach layer refuses:
  // the per-node attach ceiling (a scheduling-time refusal, the Pod never gets a node), the
  // multi-attach error (a placed Pod cannot get an RWO disk off a live peer), and detach-on-node-loss
  // (a placed Pod cannot get it off a silent peer). Those three run in order of how far the Pod got.
  {
    id: 'storage-csi-architecture',
    title: 'CSI Architecture',
    category: 'storage',
    subcategory: 'csi',
    desc: 'Kubernetes core has no code for any specific storage vendor, so how does an EBS or Ceph disk ever get created and mounted? A CSI driver ships in two halves. The controller plugin is a Deployment whose sidecars each watch one kind of object, external-provisioner for claims and external-attacher for attachments, and turn it into one gRPC call to the vendor driver. The node plugin is a DaemonSet on every node that registers with kubelet and is the only component ever to touch the node filesystem, so the sidecars bridge plain Kubernetes objects to a vendor it was never taught about.',
    k8sVersion: '1.35',
    module: './schemes/storage-csi-architecture.js',
    tinted: true,
    sources: [
      { label: 'CSI Volumes', href: 'https://kubernetes.io/docs/concepts/storage/volumes/#csi' },
      { label: 'Storage Concepts', href: 'https://kubernetes.io/docs/concepts/storage/' },
    ],
  },
  {
    id: 'storage-csi-attach-mount',
    title: 'Attach and Mount Chain',
    category: 'storage',
    subcategory: 'csi',
    desc: 'A bound claim is not a mounted disk, so what actually happens between the two? Four gRPC calls run in order: CreateVolume makes the disk exist, ControllerPublishVolume attaches it to the node at the cloud level, NodeStageVolume formats it if needed and mounts it once at a global staging path, and NodePublishVolume bind-mounts that staged filesystem into the Pod directory as /data. Stage happens once per node while publish happens once per Pod, which is exactly how several Pods on one node share a single attached disk. The split that looks redundant is what makes sharing possible.',
    k8sVersion: '1.35',
    module: './schemes/storage-csi-attach-mount.js',
    tinted: true,
    sources: [
      { label: 'CSI Volumes', href: 'https://kubernetes.io/docs/concepts/storage/volumes/#csi' },
      { label: 'Persistent Volumes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
    ],
  },
  {
    id: 'storage-volumeattachment',
    title: 'VolumeAttachment',
    category: 'storage',
    subcategory: 'csi',
    desc: 'What is the cluster record that a disk is attached to a node, and who writes it? Not the Pod and not kubelet: the attach and detach controller inside kube-controller-manager decides a volume must be attached and writes a VolumeAttachment object. The external-attacher watches those objects, calls ControllerPublishVolume, and on success sets status.attached to true, and only then does kubelet proceed to mount. Because that object, not the Pod, is the record of the attach, deleting it is what triggers the detach.',
    k8sVersion: '1.35',
    module: './schemes/storage-volumeattachment.js',
    tinted: true,
    sources: [
      { label: 'VolumeAttachment API', href: 'https://kubernetes.io/docs/reference/kubernetes-api/config-and-storage-resources/volume-attachment-v1/' },
      { label: 'Persistent Volumes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
    ],
  },
  {
    id: 'storage-mount-path-chain',
    title: 'Where the Bytes Land',
    category: 'storage',
    subcategory: 'csi',
    desc: 'A container writes to /data, but where do the bytes actually go on the node? The path is a short chain of mounts: the raw block device is mounted once at a global staging path, that single staged filesystem is bind-mounted into each Pod private directory, and the runtime maps that directory to /data inside the container. Two Pods on the same node get two separate bind mounts onto the one staged device, never a second attach or a copy. Follow a write back down and it passes through the bind mount into the staging mount and onto the same disk.',
    k8sVersion: '1.35',
    module: './schemes/storage-mount-path-chain.js',
    tinted: true,
    sources: [
      { label: 'CSI Volumes', href: 'https://kubernetes.io/docs/concepts/storage/volumes/#csi' },
      { label: 'Persistent Volumes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
    ],
  },
  {
    id: 'storage-fsgroup-ownership',
    title: 'fsGroup and Volume Ownership',
    category: 'storage',
    subcategory: 'csi',
    desc: 'A freshly mounted volume is owned by root, so why can a non-root container not write to it, and what fixes that? securityContext.fsGroup names a GID, and kubelet chowns and setgids the whole volume tree to that group before the container starts, adding every container to it so writes succeed. That walk is cheap on a small volume but can add minutes to each Pod start on a volume with millions of files. fsGroupChangePolicy OnRootMismatch checks only the top-level directory and skips the walk when it already matches, while the default Always walks the entire tree every single start.',
    k8sVersion: '1.35',
    module: './schemes/storage-fsgroup-ownership.js',
    tinted: true,
    sources: [
      { label: 'Security Context', href: 'https://kubernetes.io/docs/tasks/configure-pod-container/security-context/' },
      { label: 'Volume Permission and Ownership Change Policy', href: 'https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#configure-volume-permission-and-ownership-change-policy-for-pods' },
    ],
  },
  {
    id: 'storage-volume-attach-limits',
    title: 'Node Volume Attach Limits',
    category: 'storage',
    subcategory: 'csi',
    desc: 'Why does a Pod stay Pending with node(s) exceed max volume count while every node still has spare CPU and memory? Because a node has a second, invisible capacity: the number of volumes one CSI driver may have attached to it at once. The node plugin answers NodeGetInfo with max_volumes_per_node, kubelet writes it into that node CSINode object as allocatable.count, and the scheduler NodeVolumeLimits filter rejects any node already at its ceiling before scoring ever runs. The sharp edge is that the filter counts live VolumeAttachments as well as Pods, so a slot is released when a detach completes and not when a Pod dies, which turns every rollout near the ceiling into a race.',
    k8sVersion: '1.35',
    module: './schemes/storage-volume-attach-limits.js',
    tinted: true,
    sources: [
      { label: 'CSINode API', href: 'https://kubernetes.io/docs/reference/kubernetes-api/config-and-storage-resources/csi-node-v1/' },
      { label: 'Node-specific Volume Limits', href: 'https://kubernetes.io/docs/concepts/storage/storage-limits/' },
    ],
  },
  {
    id: 'storage-multi-attach-error',
    title: 'Multi-Attach Error',
    category: 'storage',
    subcategory: 'csi',
    desc: 'Why does a Pod sometimes hang in ContainerCreating with a Multi-Attach error for its volume? An RWO disk can be attached to only one node at a time, and RollingUpdate stands the replacement Pod up on a new node while the old one is still running and still holding the attachment. Nothing here is broken and no node is unhealthy: the controller simply will not write a second VolumeAttachment, and the rollout will not delete the old Pod until the new one reports ready, so each side waits on the other. Switching that Deployment to Recreate breaks the circle by deleting first, which is what a StatefulSet already does.',
    k8sVersion: '1.35',
    module: './schemes/storage-multi-attach-error.js',
    tinted: true,
    sources: [
      { label: 'Access Modes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes' },
      { label: 'Deployment Strategy', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#strategy' },
    ],
  },
  {
    id: 'storage-volume-detach-on-node-loss',
    title: 'Detach on Node Failure',
    category: 'storage',
    subcategory: 'csi',
    desc: 'When a node goes NotReady and its kubelet falls silent, why does the volume not just move to a healthy node right away? Nothing is contending for it here: the wait is on doubt rather than on a rival claim. The old Pod cannot be confirmed dead, and detaching a disk while it might still be writing risks corrupting the filesystem, so Kubernetes deliberately rides out the pod-eviction timeout and then the roughly six minute force-detach before attaching on the new node, a safety property rather than a bug. When an operator knows the node is truly dead, the non-graceful node shutdown out-of-service taint is the escape hatch that skips the wait.',
    k8sVersion: '1.35',
    module: './schemes/storage-volume-detach-on-node-loss.js',
    tinted: true,
    sources: [
      { label: 'Non-graceful Node Shutdown', href: 'https://kubernetes.io/docs/concepts/cluster-administration/node-shutdown/#non-graceful-node-shutdown' },
      { label: 'Persistent Volumes', href: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
    ],
  },
  // Stateful Data is the operations layer: per-replica claims and their retention, the two ways the
  // scheduler stays storage-aware (topology binding and capacity tracking), snapshots and clones, and
  // finally inline generic ephemeral volumes, which close the loop back to the ephemeral foundations.
  {
    id: 'storage-volumeclaimtemplates',
    title: 'StatefulSet volumeClaimTemplates',
    category: 'storage',
    subcategory: 'stateful-data',
    desc: 'Why does every replica of a StatefulSet get its own disk while a Deployment makes them all share one? The answer is the volumeClaimTemplate, which stamps out one PersistentVolumeClaim per ordinal with a deterministic name like data-web-0 rather than a random one. Because the name is derived from the Pod identity, a Pod that is deleted and recreated, even on another node, rebinds the very same claim and the very same data. The claims are never deleted with their Pods, so scaling up mints new ones and scaling down quietly leaves them behind.',
    k8sVersion: '1.35',
    module: './schemes/storage-volumeclaimtemplates.js',
    tinted: true,
    sources: [
      { label: 'StatefulSet Components', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/#components' },
      { label: 'Stable Storage', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/#stable-storage' },
    ],
  },
  {
    id: 'storage-pvc-retention-policy',
    title: 'StatefulSet PVC Retention',
    category: 'storage',
    subcategory: 'stateful-data',
    desc: 'When a StatefulSet shrinks or is deleted, what happens to the disks it leaves behind? The persistentVolumeClaimRetentionPolicy answers that with two independent knobs: whenScaled for a replica that is scaled away and whenDeleted for the whole set being removed, each set to Retain or Delete. Retain on both is the historical default, which is safe but silently leaks a disk on every scale-down that nobody cleans up. Switch a knob to Delete and Kubernetes reclaims the claim and its volume automatically, at the cost of that data being gone for good.',
    k8sVersion: '1.35',
    module: './schemes/storage-pvc-retention-policy.js',
    tinted: true,
    sources: [
      { label: 'PVC Retention Policy', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/#persistentvolumeclaim-retention' },
      { label: 'StatefulSets', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/' },
    ],
  },
  {
    id: 'storage-topology-aware-provisioning',
    title: 'WaitForFirstConsumer',
    category: 'storage',
    subcategory: 'stateful-data',
    desc: 'Why does a Pod sometimes hang forever in a multi-zone cluster with a perfectly healthy disk sitting right there? With volumeBindingMode Immediate the volume is provisioned the instant the claim exists, in whatever zone the provisioner picks, and the scheduler then finds no node that both fits the Pod and lies in the disk zone, so the Pod stays Pending with a volume node affinity conflict. WaitForFirstConsumer inverts the order: binding waits until a Pod is actually scheduled, so the scheduler chooses the node first and the volume is provisioned in that same topology. It is the single most common multi-zone storage bug and its one-line fix.',
    k8sVersion: '1.35',
    module: './schemes/storage-topology-aware-provisioning.js',
    tinted: true,
    sources: [
      { label: 'Volume Binding Mode', href: 'https://kubernetes.io/docs/concepts/storage/storage-classes/#volume-binding-mode' },
      { label: 'Allowed Topologies', href: 'https://kubernetes.io/docs/concepts/storage/storage-classes/#allowed-topologies' },
    ],
  },
  {
    id: 'storage-csi-capacity-tracking',
    title: 'CSI Storage Capacity',
    category: 'storage',
    subcategory: 'stateful-data',
    desc: 'Why does a Pod get stuck Pending forever when the local storage pool on its chosen node is already full? Without capacity information the scheduler picks that node on cpu and memory alone, provisioning of the volume then fails there, and because binding waits on provisioning the Pod never actually schedules. CSIStorageCapacity objects, published by the driver for each topology segment, let the scheduler see the free space per node and filter out the ones that cannot fit the claim before it commits. The permanent hang becomes a clean placement, simply by letting the scheduler look before it leaps.',
    k8sVersion: '1.35',
    module: './schemes/storage-csi-capacity-tracking.js',
    tinted: true,
    sources: [
      { label: 'Storage Capacity', href: 'https://kubernetes.io/docs/concepts/storage/storage-capacity/' },
      { label: 'CSIStorageCapacity API', href: 'https://kubernetes.io/docs/concepts/storage/storage-capacity/#api' },
    ],
  },
  {
    id: 'storage-volume-snapshot',
    title: 'Volume Snapshots',
    category: 'storage',
    subcategory: 'stateful-data',
    desc: 'How do you capture a point-in-time copy of a live volume, and why is that copy not a backup? A VolumeSnapshot is the namespaced request and a VolumeSnapshotClass names the driver. The snapshot controller creates the cluster-scoped VolumeSnapshotContent and binds it exactly as a PV binds to a PVC, and that object is what wakes the CSI snapshotter sidecar into calling CreateSnapshot. Once readyToUse flips true, a new PVC with a dataSource seeds a fresh volume from it. But the snapshot lives in the same storage system as the original, so if that system fails they both go with it.',
    k8sVersion: '1.35',
    module: './schemes/storage-volume-snapshot.js',
    tinted: true,
    sources: [
      { label: 'Volume Snapshots', href: 'https://kubernetes.io/docs/concepts/storage/volume-snapshots/' },
      { label: 'VolumeSnapshotClasses', href: 'https://kubernetes.io/docs/concepts/storage/volume-snapshot-classes/' },
      { label: 'Snapshot Controller', href: 'https://kubernetes-csi.github.io/docs/snapshot-controller.html' },
      { label: 'CSI external-snapshotter', href: 'https://kubernetes-csi.github.io/docs/external-snapshotter.html' },
    ],
  },
  {
    id: 'storage-pvc-clone',
    title: 'Cloning a PVC',
    category: 'storage',
    subcategory: 'stateful-data',
    desc: 'Need an independent copy of a volume right now, without setting up a snapshot first? A clone is a new PVC whose dataSource points straight at an existing PVC, and the storage system makes an exact duplicate server-side with no snapshot object in between. It only works within limits: the two claims must share a namespace and a volumeMode, the destination must ask for at least the size of the source, and the source must be bound and not in use, though the StorageClass may differ. The result is a fully independent volume, which is exactly the difference from a snapshot restore that keeps an intermediate object around.',
    k8sVersion: '1.35',
    module: './schemes/storage-pvc-clone.js',
    tinted: true,
    sources: [
      { label: 'Volume Cloning', href: 'https://kubernetes.io/docs/concepts/storage/volume-pvc-datasource/' },
      { label: 'CSI Volume Cloning', href: 'https://kubernetes.io/docs/concepts/storage/volume-pvc-datasource/#provisioning' },
    ],
  },
  {
    id: 'storage-generic-ephemeral-volume',
    title: 'Generic Ephemeral Volumes',
    category: 'storage',
    subcategory: 'stateful-data',
    desc: 'What if you want a scratch volume that behaves like emptyDir but can be large, on a specific StorageClass, and even snapshotted? A generic ephemeral volume is an inline volumeClaimTemplate written on the Pod under ephemeral, and it gets a real PVC, real dynamic provisioning, and a real CSI mount. Its lifetime is the Pod: the PVC carries an ownerReference back to the Pod and is garbage-collected the moment the Pod is deleted. It is the bridge between throwaway node storage and the full persistent-volume machinery.',
    k8sVersion: '1.35',
    module: './schemes/storage-generic-ephemeral-volume.js',
    tinted: true,
    sources: [
      { label: 'Generic Ephemeral Volumes', href: 'https://kubernetes.io/docs/concepts/storage/ephemeral-volumes/#generic-ephemeral-volumes' },
      { label: 'Ephemeral Volumes', href: 'https://kubernetes.io/docs/concepts/storage/ephemeral-volumes/' },
    ],
  },
  {
    id: 'control-plane-architecture',
    title: 'Control Plane Architecture',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'What are the moving parts of a Kubernetes cluster, and how do they talk to each other? The control plane holds the brain: an Api that is the single front door, an ETCD store behind it, plus the controllers and scheduler that drive the cluster toward its desired state. Every worker Node runs a Kubelet that watches the Api and brings Pods to life, so nothing ever reaches ETCD directly.',
    k8sVersion: '1.35',
    module: './schemes/control-plane-architecture.js',
    sources: [
      { label: 'Components', href: 'https://kubernetes.io/docs/concepts/overview/components/' },
      { label: 'Architecture', href: 'https://kubernetes.io/docs/concepts/architecture/' },
    ],
  },
  {
    id: 'control-etcd-raft',
    title: 'ETCD Raft Consensus',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'How does Kubernetes keep its single source of truth safe when a machine can fail? ETCD runs a small group of replicas that agree on every write through the Raft protocol: one Leader takes the change, copies it to the Followers, and commits only once a majority has stored it. That quorum rule lets the cluster survive losing a Node without ever serving a half-written state.',
    k8sVersion: '1.35',
    module: './schemes/control-etcd-raft.js',
    sources: [
      { label: 'Raft Algorithm', href: 'https://raft.github.io/' },
      { label: 'Raft Paper', href: 'https://raft.github.io/raft.pdf' },
    ],
  },
  {
    id: 'control-leader-election',
    title: 'Leader Election via Lease',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'When you run several copies of a controller for high availability, how do they avoid all acting at once? They race to claim a single Lease object, and only the winner runs the control loops while the rest stand by. The holder keeps renewing the Lease to prove it is alive, and the moment those renewals stop a standby acquires it, so leadership moves over automatically after a crash.',
    k8sVersion: '1.35',
    module: './schemes/control-leader-election.js',
    sources: [
      { label: 'Leases', href: 'https://kubernetes.io/docs/concepts/architecture/leases/' },
      { label: 'Lease v1', href: 'https://kubernetes.io/docs/reference/kubernetes-api/cluster-resources/lease-v1/' },
    ],
  },
  {
    id: 'control-scheduler-decision',
    title: 'Scheduler Decision Cycle',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'A new Pod has no Node yet, so how does Kubernetes decide where it runs? The scheduler pulls the Pod off its queue, filters out every Node that cannot fit it, scores the survivors to find the best home, and writes that choice back as a binding. From there the Kubelet on the chosen Node takes over and starts the Pod.',
    k8sVersion: '1.35',
    module: './schemes/control-scheduler-decision.js',
    sources: [
      { label: 'Kube-scheduler', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/kube-scheduler/' },
      { label: 'Scheduling Framework', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/scheduling-framework/' },
    ],
  },
  {
    id: 'control-admission-webhooks',
    title: 'Admission Chain',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'Before any object reaches storage, what gets a say in whether it is allowed and how it looks? Each request runs a gauntlet inside the Api: it is authenticated, authorized, optionally rewritten, checked against the schema, and validated one last time before the write commits. The mutating and validating steps are where policy tools like Kyverno, OPA Gatekeeper or sidecar injectors plug in.',
    k8sVersion: '1.35',
    module: './schemes/control-admission-webhooks.js',
    sources: [
      { label: 'Admission Controllers', href: 'https://kubernetes.io/docs/reference/access-authn-authz/admission-controllers/' },
    ],
  },
  {
    id: 'control-api-structure',
    title: 'List-Watch and Informers',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'How does a controller keep an up-to-date picture of the cluster without hammering the Api? It lists the objects it cares about once, then opens a watch that streams every later change. An informer turns that stream into a local cache, so the controller reconciles against memory and only re-lists when the watch falls too far behind.',
    k8sVersion: '1.35',
    module: './schemes/control-api-structure.js',
    sources: [
      { label: 'API Concepts', href: 'https://kubernetes.io/docs/reference/using-api/api-concepts/' },
      { label: 'Kubernetes API', href: 'https://kubernetes.io/docs/concepts/overview/kubernetes-api/' },
    ],
  },
  {
    id: 'control-plane-apply-flow',
    title: 'Kubectl Apply Flow',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'What actually happens between kubectl apply and a running Pod? The manifest travels to the Api and lands in ETCD, then a chain of watchers takes over: a controller turns the Deployment into a Pod, the scheduler assigns it a Node, and the Kubelet there starts the container. Each handoff is just one component reacting to a change it saw on its watch.',
    k8sVersion: '1.35',
    module: './schemes/control-plane-apply-flow.js',
    sources: [
      { label: 'Architecture', href: 'https://kubernetes.io/docs/concepts/architecture/' },
    ],
  },
  {
    id: 'control-plane-delete-flow',
    title: 'Kubectl Delete Flow',
    category: 'control',
    subcategory: 'control-plane',
    desc: 'Deleting an object is not as instant as it looks, so what runs behind the scenes? The Api first only marks the object for deletion and lets finalizers and the garbage collector do their work, walking ownerReferences to remove the dependents in order. Only once everything is cleaned up and the finalizers clear does the record actually leave ETCD.',
    k8sVersion: '1.35',
    module: './schemes/control-plane-delete-flow.js',
    sources: [
      { label: 'Garbage Collection', href: 'https://kubernetes.io/docs/concepts/architecture/garbage-collection/' },
      { label: 'Owners and Dependents', href: 'https://kubernetes.io/docs/concepts/overview/working-with-objects/owners-dependents/' },
      { label: 'Finalizers', href: 'https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/' },
    ],
  },
  {
    id: 'control-node-drain',
    title: 'Node Drain',
    category: 'control',
    subcategory: 'worker-nodes',
    desc: 'How do you safely take a Node out of service without dropping your apps? kubectl drain first cordons the Node so nothing new lands on it, then evicts the existing Pods one by one through the Eviction API. A PodDisruptionBudget can hold an eviction back with a 429 until a replacement is ready elsewhere, and DaemonSet Pods are left in place.',
    k8sVersion: '1.35',
    module: './schemes/control-node-drain.js',
    sources: [
      { label: 'Safely Drain a Node', href: 'https://kubernetes.io/docs/tasks/administer-cluster/safely-drain-node/' },
      { label: 'Eviction API', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/api-eviction/' },
      { label: 'PodDisruptionBudget', href: 'https://kubernetes.io/docs/concepts/workloads/pods/disruptions/' },
    ],
  },
  {
    id: 'control-kubelet-sync-loop',
    title: 'Kubelet Reconcile Loop',
    category: 'control',
    subcategory: 'worker-nodes',
    desc: 'How does the Kubelet turn a Pod spec into running containers, and keep them that way? It runs a loop that compares the Pods it watches from the Api against what the container runtime reports is actually there. Whenever the two differ it issues the runtime calls needed to converge, then reports the fresh status back, over and over for the life of the Node.',
    k8sVersion: '1.35',
    module: './schemes/control-kubelet-sync-loop.js',
    sources: [
      { label: 'Kubelet', href: 'https://kubernetes.io/docs/concepts/overview/components/#kubelet' },
      { label: 'Kubelet PLEG', href: 'https://developers.redhat.com/blog/2019/11/13/pod-lifecycle-event-generator-understanding-the-pleg-is-not-healthy-issue-in-kubernetes' },
    ],
  },
  {
    id: 'control-node-pressure-eviction',
    title: 'Node-pressure Eviction',
    category: 'control',
    subcategory: 'worker-nodes',
    desc: 'When a Node starts running low on memory or disk, who decides which Pods to sacrifice? The Kubelet watches local usage and, once it crosses a configured threshold, ranks the Pods by their QoS class and evicts the cheapest ones first. It keeps terminating victims until the pressure clears, which is how one greedy Pod is stopped from taking the whole Node down with it.',
    k8sVersion: '1.35',
    module: './schemes/control-node-pressure-eviction.js',
    sources: [
      { label: 'Node-Pressure Eviction', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/node-pressure-eviction/' },
      { label: 'Pod QoS Classes', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-qos/' },
    ],
  },
  {
    id: 'control-graceful-node-shutdown',
    title: 'Graceful Node Shutdown',
    category: 'control',
    subcategory: 'worker-nodes',
    desc: 'When a Node is told to power off, can its Pods still shut down cleanly first? With graceful Node shutdown enabled, the Kubelet catches the systemd signal and holds the shutdown back while it terminates Pods in priority order, giving ordinary workloads a grace window and critical ones their own. Only after that does it release the lock and let the operating system finish powering down.',
    k8sVersion: '1.35',
    module: './schemes/control-graceful-node-shutdown.js',
    sources: [
      { label: 'Graceful Node Shutdown', href: 'https://kubernetes.io/docs/concepts/cluster-administration/node-shutdown/' },
      { label: 'KEP-2000: Graceful Node Shutdown', href: 'https://github.com/kubernetes/enhancements/tree/master/keps/sig-node/2000-graceful-node-shutdown' },
    ],
  },
  {
    id: 'control-node-failure',
    title: 'Node Failure and Eviction',
    category: 'control',
    subcategory: 'worker-nodes',
    desc: 'A Node goes silent, so how long until its Pods come back elsewhere? The cluster notices when the heartbeat Lease stops renewing, flips the Node to an unknown state, and taints it so the Pods are eventually evicted and rescheduled. The grace period plus the default toleration mean recovery takes a few minutes by design, trading raw speed for not overreacting to a brief network blip.',
    k8sVersion: '1.35',
    module: './schemes/control-node-failure.js',
    sources: [
      { label: 'Node Status', href: 'https://kubernetes.io/docs/concepts/architecture/nodes/#node-status' },
      { label: 'Node Taints', href: 'https://kubernetes.io/docs/reference/labels-annotations-taints/#node-kubernetes-io-unreachable' },
      { label: 'Taint-Based Evictions', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/#taint-based-evictions' },
    ],
  },
  {
    id: 'control-pod-sandbox-cri',
    title: 'Pod Sandbox via CRI',
    category: 'control',
    subcategory: 'worker-nodes',
    desc: 'What does the container runtime actually do when the Kubelet asks it to start a Pod? It first builds a sandbox, a tiny pause container that owns the shared network and namespaces, then has the CNI plugin wire up the Pod IP. Only after the images are pulled does it create and start each workload container inside that sandbox, so every container in the Pod shares one network identity.',
    k8sVersion: '1.35',
    module: './schemes/control-pod-sandbox-cri.js',
    sources: [
      { label: 'CRI Spec', href: 'https://github.com/kubernetes/cri-api/blob/master/pkg/apis/runtime/v1/api.proto' },
      { label: 'Container Runtimes', href: 'https://kubernetes.io/docs/setup/production-environment/container-runtimes/' },
    ],
  },
  {
    id: 'control-oom-kill',
    title: 'Container OOMKill',
    category: 'control',
    subcategory: 'worker-nodes',
    desc: 'A container blows past its memory limit, so what kills it and what gets recorded? The limit is enforced by the kernel cgroup, and when usage hits the cap the kernel OOM killer sends SIGKILL, ending the process with exit code 137. The Kubelet only witnesses it: it records the OOMKilled reason in the container status and then restarts the container according to its restart policy.',
    k8sVersion: '1.35',
    module: './schemes/control-oom-kill.js',
    sources: [
      { label: 'Resource Management', href: 'https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/' },
      { label: 'Pod QoS Classes', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-qos/' },
    ],
  },
  {
    id: 'workloads-replicaset',
    title: 'ReplicaSet Reconcile and Ownership',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'How does Kubernetes keep exactly the right number of identical Pods alive? A ReplicaSet runs a control loop that constantly compares how many Pods it should have against how many it can see, creating or deleting Pods until the two match. It owns those Pods through ownerReferences, adopts any stray Pod whose labels match its selector, and lets go of one whose labels change.',
    k8sVersion: '1.35',
    module: './schemes/workloads-replicaset.js',
    tinted: true,
    sources: [
      { label: 'ReplicaSet', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/' },
      { label: 'Owners and Dependents', href: 'https://kubernetes.io/docs/concepts/overview/working-with-objects/owners-dependents/' },
    ],
  },
  {
    id: 'workloads-statefulset-ordered-startup',
    title: 'StatefulSet Ordered Rollout',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'Some workloads care about the order their Pods come up in. A StatefulSet starts its Pods one at a time in a fixed sequence, waiting for each to become ready before creating the next, and gives every Pod a stable name and its own storage that follow it across restarts. When it scales down, it tears the Pods down in reverse, newest first.',
    k8sVersion: '1.35',
    module: './schemes/workloads-statefulset-ordered-startup.js',
    tinted: true,
    sources: [
      { label: 'StatefulSets', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/' },
    ],
  },
  {
    id: 'workloads-init-containers-and-sidecars',
    title: 'Init Containers and Native Sidecars',
    category: 'workloads',
    subcategory: 'pods-bootstrap',
    desc: 'How does a Pod start its containers when some must run before others? Init containers run strictly in order, each exiting 0 before the next begins. A native sidecar (an initContainer with restartPolicy=Always, GA in 1.33) then runs for the whole Pod lifetime, holding main back until it reports Started, and on shutdown the order reverses.',
    k8sVersion: '1.35',
    module: './schemes/workloads-init-containers-and-sidecars.js',
    tinted: true,
    sources: [
      { label: 'Sidecar Containers', href: 'https://kubernetes.io/docs/concepts/workloads/pods/sidecar-containers/' },
    ],
  },
  {
    id: 'workloads-pod-image-pull',
    title: 'Image Pull and Registry Auth',
    category: 'workloads',
    subcategory: 'pods-bootstrap',
    desc: 'How does the Kubelet get a container image onto the Node before it can run? It reads imagePullPolicy (Always for :latest, IfNotPresent for fixed tags, Never for preloaded), uses imagePullSecrets for the registry, and pulls only the layers it does not already have by sha256 digest into a shared store. A failed pull surfaces as ErrImagePull and retries on an ImagePullBackOff.',
    k8sVersion: '1.35',
    module: './schemes/workloads-pod-image-pull.js',
    tinted: true,
    sources: [
      { label: 'Images', href: 'https://kubernetes.io/docs/concepts/containers/images/' },
      { label: 'ImagePullSecrets', href: 'https://kubernetes.io/docs/concepts/configuration/secret/#using-imagepullsecrets' },
      { label: 'OCI Distribution Spec', href: 'https://github.com/opencontainers/distribution-spec' },
    ],
  },
  {
    id: 'workloads-pod-qos-classes',
    title: 'Pod QoS Classes',
    category: 'workloads',
    subcategory: 'pods-bootstrap',
    desc: 'When a Node runs low on memory, which Pods does Kubernetes sacrifice first? At admission it reads each Pod resources block and assigns a QoS class: Guaranteed when requests equal limits, BestEffort when nothing is set, Burstable in between. The class is fixed for the Pod life and drives cgroup limits, oom_score_adj and the node-pressure eviction order.',
    k8sVersion: '1.35',
    module: './schemes/workloads-pod-qos-classes.js',
    tinted: true,
    sources: [
      { label: 'Pod QoS Classes', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-qos/' },
      { label: 'Resource Management', href: 'https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/' },
      { label: 'Node-Pressure Eviction', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/node-pressure-eviction/' },
    ],
  },
  {
    id: 'workloads-pod-priority-preemption',
    title: 'Pod Priority and Preemption',
    category: 'workloads',
    subcategory: 'pods-bootstrap',
    desc: 'What happens when an important Pod has nowhere to fit on a full cluster? Its PriorityClass resolves to a numeric priority, and when every Node fails on capacity the scheduler preempts: it picks the smallest set of lower-priority Pods whose removal makes room and deletes them with a plain DELETE, bypassing the PodDisruptionBudgets normal eviction respects. The high-priority Pod is then bound in their place.',
    k8sVersion: '1.35',
    module: './schemes/workloads-pod-priority-preemption.js',
    tinted: true,
    sources: [
      { label: 'Priority and Preemption', href: 'https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/' },
      { label: 'PriorityClass', href: 'https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.35/#priorityclass-v1-scheduling-k8s-io' },
    ],
  },
  {
    id: 'workloads-pvc-stickiness',
    title: 'StatefulSet PVC Stickiness',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'When a StatefulSet Pod moves to another node, does its data move with it? Yes. Each Pod owns a volume tied to its identity, and that volume outlives the Pod, so when a replacement with the same identity starts up, possibly on a different node, it reattaches the very same disk and sees exactly the data the previous Pod left behind.',
    k8sVersion: '1.35',
    module: './schemes/workloads-pvc-stickiness.js',
    tinted: true,
    sources: [
      { label: 'StatefulSet Basics', href: 'https://kubernetes.io/docs/tutorials/stateful-application/basic-stateful-set/' },
    ],
  },
  {
    id: 'workloads-daemonset',
    title: 'DaemonSet Controller',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'How do you run a copy of the same Pod on every node, and exactly one? A DaemonSet watches the set of nodes and keeps one Pod on each that matches, adding a Pod when a node joins and removing it when a node leaves, so the Pod count tracks the cluster instead of a fixed number of replicas.',
    k8sVersion: '1.35',
    module: './schemes/workloads-daemonset.js',
    tinted: true,
    sources: [
      { label: 'DaemonSet', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/' },
    ],
  },
  {
    id: 'workloads-job-parallelism',
    title: 'Job Parallelism and Completions',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'A Job runs a batch of work to completion instead of serving forever. You tell it how many successful runs you need in total and how many Pods may run at the same time, and it keeps launching Pods and retrying the ones that fail until that target is reached. Once enough runs have succeeded it stops on its own and marks the batch complete.',
    k8sVersion: '1.35',
    module: './schemes/workloads-job-parallelism.js',
    tinted: true,
    sources: [
      { label: 'Jobs', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/job/' },
    ],
  },
  {
    id: 'workloads-cronjob',
    title: 'CronJob Schedule and Concurrency',
    category: 'workloads',
    subcategory: 'controllers',
    desc: 'How do you run a Job on a repeating schedule instead of on demand? A CronJob holds a cron expression and, each time the clock matches, creates one Job from its template, which in turn runs a Pod. It also decides what to do when a run is still going at the next tick, prunes old finished Jobs so history does not pile up, and skips runs that were missed while the controller was down.',
    k8sVersion: '1.35',
    module: './schemes/workloads-cronjob.js',
    tinted: true,
    sources: [
      { label: 'CronJob', href: 'https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/' },
      { label: 'Running Automated Tasks with a CronJob', href: 'https://kubernetes.io/docs/tasks/job/automated-tasks-with-cron-jobs/' },
    ],
  },
  {
    id: 'workloads-pod-phase-machine',
    title: 'Pod Lifecycle Phases',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'The life of a Pod unfolds in a few clear stages: it starts up on a node, runs while its containers do their work, and eventually finishes, either cleanly or with a failure. The phase field is the single high-level line that tells you which stage the Pod is in right now.',
    k8sVersion: '1.35',
    module: './schemes/workloads-pod-phase-machine.js',
    tinted: true,
    sources: [
      { label: 'Pod Lifecycle', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/' },
    ],
  },
  {
    id: 'workloads-restart-policy',
    title: 'Pod restartPolicy: Always, OnFailure, Never',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'When a container in a Pod exits, what should happen next? restartPolicy is the Pod-level answer Kubernetes follows: always bring the container back, only retry when it failed, or leave it alone. The choice shapes whether the Pod runs forever, retries until success, or finishes after one go.',
    k8sVersion: '1.35',
    module: './schemes/workloads-restart-policy.js',
    tinted: true,
    sources: [
      { label: 'Restart Policy', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy' },
    ],
  },
  {
    id: 'workloads-hooks',
    title: 'Lifecycle Hooks: postStart and preStop',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'Sometimes a container needs to do something right when it starts, or right before it stops. Lifecycle hooks (postStart and preStop) are the two slots Kubernetes exposes for that code, run by the Kubelet so your container can announce itself, drain connections, or wrap up cleanly around its main work.',
    k8sVersion: '1.35',
    module: './schemes/workloads-hooks.js',
    tinted: true,
    sources: [
      { label: 'Container Lifecycle Hooks', href: 'https://kubernetes.io/docs/concepts/containers/container-lifecycle-hooks/' },
    ],
  },
  {
    id: 'workloads-probes',
    title: 'Startup, Liveness and Readiness Probes',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'Three Kubelet handlers (httpGet, tcpSocket, grpc, exec) on periodSeconds intervals. startupProbe gates the other two during a slow boot, livenessProbe restarts the container after failureThreshold consecutive failures, readinessProbe toggles the Pod IP in the Service EndpointSlice without a restart.',
    k8sVersion: '1.35',
    module: './schemes/workloads-probes.js',
    tinted: true,
    sources: [
      { label: 'Configure Liveness, Readiness and Startup Probes', href: 'https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/' },
    ],
  },
  {
    id: 'workloads-container-states',
    title: 'Container Restarts and lastState',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'A container crashed and restarted, what killed it? The live state shows the fresh Running instance and hides the failure, but Kubernetes preserves the previous termination next to it: lastState holds the exit code and reason, restartCount counts the restarts, and kubectl logs --previous reads the prior log stream.',
    k8sVersion: '1.35',
    module: './schemes/workloads-container-states.js',
    tinted: true,
    sources: [
      { label: 'Container States', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#container-states' },
    ],
  },
  {
    id: 'workloads-crashloopbackoff',
    title: 'CrashLoopBackOff and Restart Backoff',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'A container that keeps crashing gets restarted with a growing delay between attempts, doubling from 10s up to a 5 minute ceiling so a broken process cannot saturate the node by hot-looping. The container shows up as Waiting with reason CrashLoopBackOff during each wait, the Pod phase stays Running, and the timer only resets after a sustained healthy run.',
    k8sVersion: '1.35',
    module: './schemes/workloads-crashloopbackoff.js',
    tinted: true,
    sources: [
      { label: 'Restart Policy', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy' },
    ],
  },
  {
    id: 'workloads-graceful-shutdown',
    title: 'Graceful Pod Shutdown',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'When a Pod is deleted, Kubernetes does not kill it on the spot. The Kubelet runs the preStop hook, sends SIGTERM to the container, then waits out the terminationGracePeriodSeconds budget so the app can drain in-flight work, close connections and exit cleanly. SIGKILL is the last resort, used only if the container is still alive when the timer runs out.',
    k8sVersion: '1.35',
    module: './schemes/workloads-graceful-shutdown.js',
    tinted: true,
    sources: [
      { label: 'Termination of Pods', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-termination' },
    ],
  },
  {
    id: 'workloads-force-deletion',
    title: 'Force Deletion and Stuck Terminating Pods',
    category: 'workloads',
    subcategory: 'pods-lifecycle',
    desc: 'A Pod can get stuck in Terminating when the node hosting it goes silent. The Kubelet never acknowledges the delete, so the Api keeps the object around and an identity-bound controller like StatefulSet refuses to schedule a replacement. Force deletion drops the object from etcd at once, but if the node was only partitioned the original container may still run and clash with the recreated replica.',
    k8sVersion: '1.35',
    module: './schemes/workloads-force-deletion.js',
    tinted: true,
    sources: [
      { label: 'Force Delete StatefulSet Pods', href: 'https://kubernetes.io/docs/tasks/run-application/force-delete-stateful-set-pod/' },
      { label: 'Pod Lifecycle', href: 'https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-termination' },
    ],
  },
];

export const CATEGORY_LABEL = Object.fromEntries(CATEGORIES.map(c => [c.key, c.label]));

export const SUBCATEGORIES = {
  control: [
    { key: 'control-plane', label: 'Control Plane' },
    { key: 'worker-nodes',  label: 'Worker Nodes'  },
  ],
  workloads: [
    { key: 'pods-bootstrap', label: 'Pods Bootstrap' },
    { key: 'pods-lifecycle', label: 'Pods Lifecycle' },
    { key: 'controllers',   label: 'Controllers'    },
  ],
  network: [
    { key: 'foundations',        label: 'Network Foundations'    },
    { key: 'pod-networking',     label: 'Pod Networking'         },
    { key: 'service-networking', label: 'Services & Endpoints'   },
    { key: 'external-traffic',   label: 'External Traffic'       },
    { key: 'dns',                label: 'DNS & Service Discovery' },
  ],
  storage: [
    { key: 'foundations',   label: 'Volume Foundations' },
    { key: 'pv-pvc',        label: 'Volumes & Claims'   },
    { key: 'csi',           label: 'CSI & Mount Path'   },
    { key: 'stateful-data', label: 'Stateful Data'      },
  ],
};
